import test from 'node:test';import assert from 'node:assert/strict';import { readFile } from 'node:fs/promises';test('domain module registry is explicit',async()=>{const src=await readFile(new URL('../src/modules/index.ts',import.meta.url),'utf8');for(const name of ['IdentityModule','PropertiesModule','ListingsModule','MatchingModule','TrustRiskModule','AuditModule'])assert.match(src,new RegExp(name));});
