import test from 'node:test';import assert from 'node:assert/strict';import { readFile } from 'node:fs/promises';test('route registry contains critical surfaces',async()=>{const src=await readFile(new URL('../lib/routes.ts',import.meta.url),'utf8');for(const route of ['/search','/publish','/requests','/messages','/agency/','/admin/'])assert.match(src,new RegExp(route.replaceAll('/','\\/')));});
